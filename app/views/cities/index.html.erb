<div class="container col-md-10 col-md-offset-1">
  <div class="row">
    <div class="col-md-12">
      <% @cities.each do |c| %>
        <h1><%=link_to c.name, city_path(c) %></h1>
        <%= image_tag c.image, class: "img-responsive" %>
      <% end %>
    </div>
  </div>
</div>
